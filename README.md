# 南雲式WBSツール (Nagumo-style WBS Tool)

Vanilla JS + Tailwind CSS で構築された、超軽量かつ高速な完全自作ガントチャート・プロジェクト管理ツールです。

## ✨ 本ツールの圧倒的な強み

- **⚡ 超軽量・高速描画**: Vanilla JSによるスクラッチ開発。重い外部描画ライブラリに依存せず、大量のタスクを配置してもサクサク動作します。
- **🤖 AIアシスタント機能搭載**: 各種LLM（ChatGPT, Claude, Gemini等）と連携できるチャットビューを搭載し、スケジュール構築やタスク整理をAIが強力にアシストします。
- **⚡ イナズマ線 (Progress Line)**: 本日日付を基準に、進捗の遅延・順調を視覚化（遅延は赤、順調は青）。一目でプロジェクトの健康状態を把握可能です。
- **🔗 ドラッグ結線と依存関係**: タスクのコネクタからドラッグ＆ドロップするだけで、直感的にベジェ曲線の美しい依存関係（結線）を作成できます。
- **🌊 再帰的な連動押し出し機能**: 先行タスクのスケジュールが遅れて（右に移動して）後続タスクに食い込んだ場合、後続タスクとその先のタスクまで再帰的に自動シフト（ドミノシフト）します。
- **🛑 左制限 (制約制御)**: 後続タスクを過去へ移動させようとする際、先行タスクの終了日より前には配置できないようにUI上で自動クランプします。
- **🟦 複数矩形選択 (範囲選択)**: ShiftキーまたはCtrlキーを押しながらドラッグすることで範囲選択が可能。複数のタスクを一瞬で一括選択し、島ごとにまとめて移動・調整することが可能です。
- **💾 CSV / マスターデータ連携**: マスターデータ（担当者、ステータス、キャラクター、バージョン等）をUIから直感的に管理でき、データはCSVとして保存・出力されます。

---

## 🚀 インストール・起動方法

1. リポジトリをクローンまたはダウンロードします。
2. 依存ライブラリ（Python環境）をインストールします。
   ```bash
   pip install -r requirements.txt
   ```
3. アプリケーションを起動します。
   ```bash
   python app.py
   ```
   ※Windows環境の方は、付属の `WBSツール起動.bat` をダブルクリックするだけでも起動できます。
4. 自動的にブラウザが立ち上がらない場合は、ブラウザから `http://127.0.0.1:5000` にアクセスしてください。

---

## 💡 基本操作マニュアル (Usage)

本ツールは直感的かつ、プロが快適に使える操作感を追求しています。

### 1. タスクの基本操作
- **タスクの移動**: タスクをドラッグ＆ドロップして日付（左右）や行（上下）を自由に移動できます（1日単位でスナップします）。
- **期間の変更**: タスクの左右端をドラッグすることで、直感的に開発期間を伸縮できます。
- **編集・作成**: 
  - タスクのない空白領域で**右クリック** ➜ 「新規タスク作成」を選択すると、その位置のキャラクターに対してタスクを作成できます。
  - すでに存在するタスクを**ダブルクリック**、または**右クリック**から「エディタを開く」で詳細を編集できます。
- **コピー＆ペースト**: タスクを選択して `Ctrl+C` でコピー、空白部分を右クリックして `Ctrl+V` でペーストできます。
- **削除**: タスクを選択して `Delete` キーを押すか、右クリックから削除できます。

### 2. 依存関係（リンク結線）
- **タスクを繋ぐ**: 
  1. 先行タスクの右端にマウスをホバーすると、**赤い丸（コネクタ）**が出現します。
  2. その赤丸をドラッグすると、プレビュー線が伸びます。
  3. 繋げたい後続タスク（または左端のコネクタ）の上でドロップすると、美しいベジェ曲線で結線されます。
- **連動押し出し（ドミノシフト）**: 先行タスクを右（未来）へ移動して後続タスクの開始日を超えると、影響を受けるすべての後続タスクが自動的に押し出されます。
- **リンクの削除**: 依存関係を示す線を**右クリック**すると、専用メニューからその結線のみを削除できます。

### 3. 複数タスクの一括操作（範囲選択）
- `Shift` キーまたは `Ctrl` キーを押しながら、ガントチャートの空白部分をドラッグすると、**半透明のブルーの選択ボックス**が現れます。
- 囲んだタスクが一挙に選択状態（青枠）になり、ドラッグすることで**選択したすべてのタスクを同時に移動**できます。

### 4. 画面表示のカスタマイズ
- **画面のパン移動**: ガントチャートの背景をドラッグ＆ドロップすることで、画面全体を縦横にスクロールできます。
- **日付のズーム**: `マウスホイール` を回転させることで、カーソルのある位置を中心に日付の細かさ（ズームイン・ズームアウト）を変更できます。
- **UI高さの調整**: `Ctrl + マウスホイール` でガントチャートの縦幅（行の高さ）を自由に拡縮できます。
- **イナズマ線**: 画面右上の「イナズマ線」ボタンをONにすると、進捗状況（％）に応じたイナズマ線がオレンジ色の「本日（Today）」線を基準に表示されます。

### 5. AIアシスタント
- 画面右上の「AIチャット」ボタンをクリックするとチャットウィンドウが展開します。
- チャット画面右上の「設定」ボタンから、お持ちのAPIキー（OpenAI / Claude / Gemini等）を設定していただくことで、スケジュール調整やガントチャートの改善アドバイスをAIにいつでも相談できます。

---

## 💡 オープンソース公開について
本プロジェクトは、スケジュール管理の複雑さを解決し、現場のクリエイターやマネージャーが本当に「直感的に」扱える操作感を Vanilla JS で追求したツールです。皆様のプロジェクト管理業務に少しでもお役立ていただければ幸いです。