# WBSツール 開発TODO・経緯まとめ

## 過去の経緯と基盤移行の歴史
1. **初期 (Vis.js ベース)**
   - 柔軟な配置（同じレーンに複数のタスクを自由に置ける、自由な上下移動）などが強みだった。
   - しかし、DOM構造がブラックボックス化されており、マウスホバー時の行列ハイライトなどの細かい要件を追加する際にエラー（循環参照など）が多発・クラッシュする問題が発生した。

2. **DHTMLX Gantt への移行試行 (app_v2.js)**
   - より安定した高機能ライブラリとしてDHTMLXへの移行を試みた。
   - しかし、DHTMLXは「表計算ソフト的」な厳密な木構造を前提としており、「同じ行に複数のタスクを自由に配置して上下にドラッグ移動する」というVis.js時代の柔軟な操作感を再現できない（仕様上不可能に近い）ことが判明した。

3. **完全自作基盤への移行 (app_v3.js / 現在)**
   - ブラックボックス化を避け、要件を100%コントロールするために「Vanilla JS + Tailwind CSS」でガントチャート自体を完全自作する方針へ切り替えた。
   - 1日もかからず基本機能は実装できたが、ライブラリが担っていた細かい使い勝手（ズーム、スナップ、イベント判定など）が失われたため、細かな「先祖返り」が発生し、それを手動で逐一直していく作業が発生している。
   - さらに「Pythonサーバーがプロセスに居座り続け、変更が反映されない」というトラブルがあり、余計な工数とAPIコストを消費してしまった。

## 現在の状況（実装完了した機能）
- [x] 完全自作によるガント描画（`app_v3.js`）
- [x] マウス追従ハイライト（列の青色帯、行のハイライト）
- [x] 稼働Line数（ヒートマップ）のヘッダー表示
- [x] 年・月・日の3段ヘッダー表示、週番号（1W, 2W...）の計算と切り替え
- [x] タスクのテトリス配置（重なった場合のスタック表示）と隙間判定の最適化
- [x] マウスドラッグによる画面のパン移動
- [x] マウスホイールによる日付ズーム（マウス位置を焦点にする）
- [x] Ctrl+ホイールによるUI縦幅の拡縮
- [x] サーバー再起動APIとボタンの追加（二重起動バグ修正済み）
- [x] ダブルクリックでエディタ展開（親グループバーからの専用エディタ展開も対応）
- [x] 右クリックメニューおよびホットキー(Ctrl+C, V, Delete)によるタスク操作
- [x] タスク移動・リサイズ時の1日単位のカチッとしたスナップ処理

## 現在の状況（実装完了した追加要望）
- [x] 初期表示時（リロード時）にマスターエディタを開くとデフォルトプロジェクトが読み込まれるバグの修正
- [x] キャラクター選択の2段階プルダウン化（キャラクター名 → 衣装名）

## NEXT (今後の課題・直していくべき先祖返り)
- [ ] **右クリックコンテキストメニューからのエディタ起動と項目の出し分け**:
  - ダブルクリックでのエディタ展開を廃止し、コンテキストメニューからの操作に統一。
  - メニューに「親グループの作成」「子グループの作成」「タスクの作成」を追加し、選択時に直上のグループへ自動ネスト。
  - 親グループ作成時は「バージョン」のみ表示（イベントは自動表示）。
  - 子グループ・タスク作成時はすべてのフィールド（キャラクター、セクション、日付など）を表示。
  - 子グループのバージョンを変更した場合、対象の親グループ直下に移動する。
- [ ] **SourceTree風のプロジェクトタブUIの実装**:
  - タブによるプロジェクトのスマートな切り替え。
  - タブの「＋」ボタンで新規タブ作成（新規プロジェクト作成 または 既存フォルダからの読み込みを選択）。
  - タブの「×」ボタンでタブを閉じる（プロジェクトデータ自体は消さずに保持する）。
- [ ] **スクロール領域とレイアウトの最適化**: 縦横に長大になった場合のパフォーマンス担保、およびヘッダー・サイドメニューの固定表示（CSS `sticky` や `transform` の調整）。
- [ ] **その他、Vis.js時代にあった細かなUI/UXの復元**: ユーザーが操作していく中で気づく「以前はこうだった」という微細な挙動の修正。
